import {
  DialogHeader,
  DialogTitle,
} from "@/components/ui/dialog";

export function EditMemberHeader() {
  return (
    <DialogHeader>
      <DialogTitle>Edit Member</DialogTitle>
    </DialogHeader>
  );
}